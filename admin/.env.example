# Development: point at the API directly, or leave blank to use Vite proxy (/api)
VITE_API_URL=http://localhost:4000/api

# Production (admin served from same host at /admin): use relative API
# VITE_API_URL=/api
