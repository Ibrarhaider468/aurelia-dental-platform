# Local: use Vite proxy (recommended) so login avoids CORS edge cases
VITE_API_URL=/api

# Alternative: call API directly (requires CORS allowlist for http://localhost:5173)
# VITE_API_URL=http://localhost:4000/api
