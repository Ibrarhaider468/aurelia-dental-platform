# Same-origin deploy (Express serves admin at /admin): keep /api
# Cloudflare Pages / split hosting: OVERRIDE this in the host env with the
# full API URL, e.g. VITE_API_URL=https://api.yourdomain.com/api
VITE_API_URL=/api
